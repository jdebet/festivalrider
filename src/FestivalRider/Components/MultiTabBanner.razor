@if (Show)
{
    <div class="alert alert-warning mb-0 rounded-0 border-0 text-center" role="alert">
        @Message
    </div>
}

@code {
    [Parameter] public bool Show { get; set; }
    [Parameter] public string Message { get; set; } = string.Empty;
}
